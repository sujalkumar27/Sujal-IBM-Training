# Spring Boot reads this file automatically from the classpath.
# Everything here is externalized configuration — no recompile needed.

# Change the HTTP port (default is 8080)
server.port=8080

# Custom property consumed by GreetingService via @Value("${app.greeting}")
app.greeting=Hello there

# Log level tweaks (uncomment to see more)
#logging.level.org.springframework.web=DEBUG
